spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parisairline
    username: debojyotimishra
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
